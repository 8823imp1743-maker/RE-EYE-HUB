/**
 * Firebase Cloud Functions 用エントリは廃止しました（ローカル 0 円開発・VPS 移行前提）。
 *
 * 監視の単発実行:
 *   cd functions && node run-cli.mjs
 *   cd functions && node run-cli.mjs auction
 *
 * HTTP API が必要な場合は別途 Express を起動するスクリプトを用意してください。
 */

export {};
